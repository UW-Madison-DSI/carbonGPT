{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "dev-run",
            "type": "shell",
            "command": "streamlit run proto_carbongpt/main.py"
        },
        {
            "label": "docker-run",
            "type": "shell",
            "command": "docker build -t proto_carbongpt . && docker run --rm -p 8080:8080 proto_carbongpt"
        }
    ]
}